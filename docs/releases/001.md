# Version 001

状态：开发中
类型：feat
触发人：用户确认采用当前原型并继续工程化

## Initial State

- Initial commit: `1744489482045b17aa2361c5541aad95456cfefe`
- Previous successful tag: 无
- Development branch: `dev/001`
- Release branch: 尚未创建
- Rollback target: `1744489482045b17aa2361c5541aad95456cfefe`，静态原型基线

## Objective And Scope

- Objective: 将静态原型工程化为低内存、单机部署的真实只读运维控制台。
- Affected files/features: Go 后端、指标历史存储、只读应用与服务状态 API、前端真实数据接入、部署配置和技术文档。
- Out of scope: 生产部署执行、服务重启、版本回滚执行、Docker 管理权限、多服务器管理。

## Candidate Identity

- Release commit: 尚未生成
- index.html SHA-256: 尚未生成
- styles.css SHA-256: 尚未生成
- app.js SHA-256: 尚未生成

## Development Progress

- Phase A backend foundation 已完成本地实现：Go HTTP server、静态资源内嵌、配置加载、Linux `/proc` 指标采集、64-byte 固定记录、UTC 日分段存储和区间聚合 API。
- 当前提供 `GET /api/v1/health`、`GET /api/v1/overview`、`GET /api/v1/metrics`、`GET /api/v1/applications` 和 `GET /api/v1/applications/{id}/releases`。
- Phase B read-only inventory 已完成本地实现：配置驱动的应用清单、固定参数 `systemctl show` 查询、仅 loopback 的 HTTP 健康检查、JSONL 发布记录读取，以及 `GET /api/v1/applications` 和 `GET /api/v1/applications/{id}/releases`。
- 应用状态错误以 `unknown`、`unavailable` 或 `unhealthy` 返回，不伪造成功；健康检查不跟随重定向。
- 服务配置强制使用 loopback 监听；Windows 本地运行不会伪造 Linux 指标，而是保留最后成功快照并报告采集错误。

## Validation

- JavaScript syntax: 基线执行 `node --check app.js`，通过。
- Go tests: `go test ./... -count=1`，通过。
- Go vet: Windows 和 `linux/amd64` 执行 `go vet ./...`，通过。
- Linux build: `GOOS=linux GOARCH=amd64 go build .`，通过。
- Local runtime smoke test: Windows 本地启动后，静态首页、`/api/v1/health`、`/api/v1/overview` 和 `/api/v1/metrics` 均返回预期响应；Linux 指标采集按设计明确报告当前平台不支持。
- JavaScript syntax: `node --check app.js`，通过。
- Desktop browser acceptance: 1440x900 空数据/采集失败状态通过；无静态成功态残留。
- Mobile browser acceptance: 390x844 通过，`scrollWidth=390`，无横向溢出。
- Dark theme acceptance: 移动端深色主题通过。
- Console errors: 0 errors / 0 warnings。
- Deployment preparation: `deploy/origin-ops.service` 和 `docs/deployment.md` 已完成本地审查。
- Target deployment: 已在 `sub2api-cf` 安装 `/usr/local/bin/origin-ops`、`/etc/origin-ops/config.json` 和 `origin-ops.service`；服务以低权限用户运行，仅监听 `127.0.0.1:9080`。
- Target API acceptance: `/api/v1/health`、`/api/v1/overview`、`/api/v1/metrics`、`/api/v1/applications` 和发布记录读取接口均已验证。首个 CPU 采样因计数初始化失败，下一采样周期恢复成功。
- Target inventory: 基于 Caddy 路由、本地健康端点和 systemd/Docker 盘点配置 23 个应用条目与 38 个 systemd 服务；Docker 未授予 socket 访问权限，无 HTTP 健康端点的容器标记为未配置健康检查。
- Target resource acceptance: 空闲 RSS 约 11 MB；31 天查询命令的最大驻留集为 12,044 KB；指标目录当前 8 KB，满足初始资源预算。
- Unverified items: Cloudflare Access/Caddy authentication 的对外访问方案、长期指标文件恢复、真实发布记录来源和 Docker 只读 socket proxy（如需直接展示容器运行状态）。

## Result

- Final stable commit: 尚未生成
- Version tag: 尚未生成
- Known risks: 目标主机内存较小且 swap 已在使用；外部访问认证方式尚未确认；Docker 状态不可由当前 SSH 用户读取。
- Notes: 技术设计见 `docs/design/001-low-memory-ops-console.md`。版本 001 只实现读取能力，所有生产变更操作继续禁用。
